<phone:PhoneApplicationPage
    x:Class="MyFuelTracker.Views.EditFillupView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
     xmlns:toolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d"
    shell:SystemTray.IsVisible="True">
    <phone:PhoneApplicationPage.Resources>
    </phone:PhoneApplicationPage.Resources>
	<phone:PhoneApplicationPage.ApplicationBar>
		<shell:ApplicationBar>
			<!--<shell:ApplicationBarIconButton Click="OnSaveButtonClick" IconUri="/Assets/AppBar/save.png" IsEnabled="True" Text="Save"/>
            <shell:ApplicationBarIconButton Click="OnCancelButtonClick" IconUri="/Toolkit.Content/ApplicationBar.Cancel.png" IsEnabled="True" Text="Cancel"/>-->
		</shell:ApplicationBar>
	</phone:PhoneApplicationPage.ApplicationBar>
    <toolkit:TransitionService.NavigationInTransition>
        <toolkit:NavigationInTransition>
            <toolkit:NavigationInTransition.Backward>
                <toolkit:RotateTransition Mode="In180Clockwise"/>
            </toolkit:NavigationInTransition.Backward>
            <toolkit:NavigationInTransition.Forward>
                <toolkit:RotateTransition Mode="In180Counterclockwise"/>
            </toolkit:NavigationInTransition.Forward>
        </toolkit:NavigationInTransition>
    </toolkit:TransitionService.NavigationInTransition>
    <toolkit:TransitionService.NavigationOutTransition>
        <toolkit:NavigationOutTransition>
            <toolkit:NavigationOutTransition.Backward>
                <toolkit:RotateTransition Mode="Out180Clockwise"/>
            </toolkit:NavigationOutTransition.Backward>
            <toolkit:NavigationOutTransition.Forward>
                <toolkit:RotateTransition Mode="Out180Counterclockwise"/>
            </toolkit:NavigationOutTransition.Forward>
        </toolkit:NavigationOutTransition>
    </toolkit:TransitionService.NavigationOutTransition>

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--TitlePanel contains the name of the application and page title-->
        <StackPanel Grid.Row="0" Margin="12,17,0,28">
            <TextBlock Text="My Fuel Tracker" Style="{StaticResource PhoneTextNormalStyle}"/>
            <TextBlock Text="add fillup" Margin="9,-7,0,0" Style="{StaticResource PageTitle}"/>
        </StackPanel>

        <!--ContentPanel - place additional content here-->
        <Grid x:Name="ContentPanel" Grid.Row="1" Margin="12,0,12,0">
        	<Grid.RowDefinitions>
        		<RowDefinition Height="Auto"/>
        		<RowDefinition Height="Auto"/>
        		<RowDefinition Height="Auto"/>
        		<RowDefinition Height="Auto"/>
        		<RowDefinition Height="Auto"/>
        		<RowDefinition Height="Auto"/>
        	</Grid.RowDefinitions>
            
            <toolkit:DatePicker Value="{Binding FillupItemViewModel.Date, Mode=TwoWay}" VerticalAlignment="Top"/>
            <toolkit:PhoneTextBox Text="{Binding FillupItemViewModel.Odometer, Mode=TwoWay}" TextWrapping="Wrap" VerticalAlignment="Top" Hint="Odometer" Grid.Row="1" InputScope="Number"/>
            <toolkit:PhoneTextBox Text="{Binding FillupItemViewModel.Volume, Mode=TwoWay}" TextWrapping="Wrap" VerticalAlignment="Top" Hint="Volume" Grid.Row="2" InputScope="Number"/>
            <toolkit:PhoneTextBox Text="{Binding FillupItemViewModel.Price, Mode=TwoWay}" TextWrapping="Wrap" VerticalAlignment="Top" Hint="Price" Grid.Row="3" InputScope="Number"/>
        	
            <toolkit:ListPicker Grid.Row="4" VerticalContentAlignment="Stretch" VerticalAlignment="Center" Name="gasTypes">
        	</toolkit:ListPicker>
        	<CheckBox Content="Is partial fillup" Grid.Row="5"/>
               
        </Grid>
    </Grid>

</phone:PhoneApplicationPage>