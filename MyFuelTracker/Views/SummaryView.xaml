<UserControl
    x:Class="MyFuelTracker.Views.SummaryView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:bab="clr-namespace:Caliburn.Micro.BindableAppBar;assembly=Caliburn.Micro.BindableAppBar"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    mc:Ignorable="d"
    shell:SystemTray.IsVisible="True">
    <UserControl.Resources>
        <Style TargetType="TextBlock" x:Key="valueText" BasedOn="{StaticResource PhoneTextLargeStyle}">
            <Setter Property="Margin" Value="50 5 5 5"/>
            <Setter Property="Foreground" Value="{StaticResource PhoneAccentBrush}"></Setter>
        </Style>

        <Style TargetType="TextBlock" x:Key="caption" BasedOn="{StaticResource PhoneTextExtraLargeStyle}">

        </Style>
    </UserControl.Resources>
    <!--ContentPanel - place additional content here-->
    <Grid x:Name="ContentPanel" Grid.Row="1" Margin="12,0,12,0">
        <ScrollViewer>

            <StackPanel>
                <TextBlock Text="last consumption"  Style="{StaticResource caption}"/>
                    <TextBlock x:Name="LastConsumption" Style="{StaticResource valueText}"/>

                <TextBlock Text="avg consumption"  Style="{StaticResource caption}"/>
                <TextBlock x:Name="AllTimeAvgConsumption" Style="{StaticResource valueText}"/>

                <TextBlock Text="prev 4 consumption"  Style="{StaticResource caption}"/>
                <TextBlock x:Name="Last4FillupsAvgConsumption" Style="{StaticResource valueText}"/>

                <TextBlock Text="max consumption" Style="{StaticResource caption}"/>
                <TextBlock x:Name="MaxConsumption" Style="{StaticResource valueText}"/>

                <TextBlock Text="min consumption" Style="{StaticResource caption}"/>
                <TextBlock x:Name="MinConsumption" Style="{StaticResource valueText}"/>

                <TextBlock Text="avg month cost" Style="{StaticResource caption}"/>
                <TextBlock x:Name="AllTimeAvgMonthCost" Style="{StaticResource valueText}"/>

                <TextBlock Text="last month cost" Style="{StaticResource caption}"/>
                <TextBlock x:Name="LastMonthCost" Style="{StaticResource valueText}"/>
            </StackPanel>
        </ScrollViewer>
        <bab:BindableAppBar>
            <bab:BindableAppBarButton
                            x:Name="GoToAddFillup"
                            Text="add fillup"
                            IconUri="/Assets/fuel.png"/>
            <bab:BindableAppBarButton
                            x:Name="GoToSettings"
                            Text="add fillup"
                            IconUri="/Assets/feature.settings.png"/>
        </bab:BindableAppBar>
    </Grid>
</UserControl>