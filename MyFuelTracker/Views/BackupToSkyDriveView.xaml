<phone:PhoneApplicationPage
    x:Class="MyFuelTracker.Views.BackupToSkyDriveView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:live="clr-namespace:Microsoft.Live.Controls;assembly=Microsoft.Live.Controls"
    xmlns:micro="clr-namespace:Caliburn.Micro;assembly=Caliburn.Micro"
    xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
    xmlns:ec="clr-namespace:Microsoft.Expression.Interactivity.Core;assembly=Microsoft.Expression.Interactions"
    xmlns:eim="clr-namespace:Microsoft.Expression.Interactivity.Media;assembly=Microsoft.Expression.Interactions"
    xmlns:toolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d"
    shell:SystemTray.IsVisible="True"
    toolkit:TransitionService.NavigationInTransition="{StaticResource navigationInTransition}"
    toolkit:TransitionService.NavigationOutTransition="{StaticResource navigationOutTransition}">
   
    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot">
        <Grid.Background>
            <ImageBrush ImageSource="/Assets/black-wood-background.jpg"></ImageBrush>
        </Grid.Background>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--TitlePanel contains the name of the application and page title-->
        <StackPanel Grid.Row="0" Margin="12,17,0,28">
            <StackPanel Orientation="Horizontal">
                <Image Source="/Assets/fuel.png" Width="25" Height="25"></Image>
                <TextBlock Text="MY FUEL TRACKER" Style="{StaticResource PhoneTextNormalStyle}" FontWeight="Bold"/>
            </StackPanel>
            <TextBlock Text="skydrive backup" Margin="9,-7,0,0" Style="{StaticResource PhoneTextTitle1Style}"/>
        </StackPanel>

        <!--ContentPanel - place additional content here-->
        <StackPanel x:Name="ContentPanel" Grid.Row="1" Margin="12,0,12,0">
            <TextBlock  FontSize="{StaticResource PhoneFontSizeMediumLarge}" TextWrapping="Wrap" Visibility="{Binding IsSignedIn, Converter={StaticResource booleanToVisibilityConverter}, ConverterParameter=true}" Text="Please sign in and allow access to your SkyDrive."/>
            <live:SignInButton ClientId="000000004C0F1406" Branding="Skydrive" TextType="SignIn" SignInText="skydrive sign in" SignOutText="sign out"
                Scopes="wl.skydrive_update" IsEnabled="{Binding CanBackup}"
                               micro:Message.Attach="[Event SessionChanged] = [Action OnSessionChanged($eventargs)]" />
            <StackPanel x:Name="Panel">
                <StackPanel.Resources>
                    <Storyboard x:Key="ShowPanelAnimation">
                        <DoubleAnimation From="2000" To="0" Duration="00:00:00.8" Storyboard.TargetName="Panel" Storyboard.TargetProperty="(FrameworkElement.RenderTransform).(TranslateTransform.Y)" >
                            <DoubleAnimation.EasingFunction>
                                <ExponentialEase Exponent="12" EasingMode="EaseOut"/>
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                        <DoubleAnimation From="0" To="1" Duration="00:00:00.8" Storyboard.TargetName="Panel" Storyboard.TargetProperty="(FrameworkElement.Opacity)" />
                    </Storyboard>
                    <Storyboard x:Key="HidePanelAnimation" TargetName="Panel" TargetProperty="(FrameworkElement.RenderTransform).(TranslateTransform.Y)">
                        <DoubleAnimation From="{Binding HideStoryboardFrom}" To="2000" Duration="00:00:00.8" >
                            <DoubleAnimation.EasingFunction>
                                <ExponentialEase Exponent="12" EasingMode="EaseIn"/>
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                        <DoubleAnimation From="1" To="0" Duration="00:00:00.8" Storyboard.TargetName="Panel" Storyboard.TargetProperty="(FrameworkElement.Opacity)" />
                    </Storyboard>
                </StackPanel.Resources>
                <StackPanel.RenderTransform>
                    <TranslateTransform Y="2000"/>
                </StackPanel.RenderTransform>
                <i:Interaction.Triggers>
                    <ec:DataTrigger Binding="{Binding IsSignedIn}" Value="true">
                        <eim:ControlStoryboardAction ControlStoryboardOption="Play" Storyboard="{StaticResource ShowPanelAnimation}" />
                    </ec:DataTrigger>
                    <ec:DataTrigger Binding="{Binding IsSignedIn}" Value="false">
                        <eim:ControlStoryboardAction ControlStoryboardOption="Play" Storyboard="{StaticResource HidePanelAnimation}" />
                    </ec:DataTrigger>
                </i:Interaction.Triggers>
                <Rectangle Height="1" Stroke="White" Margin="0 20 0 20" />
                <TextBlock  FontSize="{StaticResource PhoneFontSizeMediumLarge}" Text="This will create a backup file on you SkyDrive into the ''MyFuelTracker' folder" TextWrapping="Wrap"/>
                <CheckBox IsEnabled="{Binding CanBackup}" FontSize="{StaticResource PhoneFontSizeMediumLarge}" Content="export history to Excel*" IsChecked="{Binding ExportToExcel, Mode=TwoWay}"/>
                <TextBlock Margin="60 0 0 0" TextWrapping="Wrap" FontSize="{StaticResource PhoneFontSizeSmall}" Text="*Actually it will be exported to a comma-separated values (CSV) file which is understood by Excel quite well"></TextBlock>
                <Button Content="backup" IsEnabled="{Binding CanBackup}" micro:Message.Attach="[Event Click] = [Action DoBackup]"></Button>
            </StackPanel>
        </StackPanel>
    </Grid>

</phone:PhoneApplicationPage>